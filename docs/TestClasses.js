//simple mock P5 script which calls relevant test functions for each class

function setup() {
    createCanvas(640, 480);
    testWeapon();
    //testTank();
}

function draw() {
    background(200, 200, 200);
}